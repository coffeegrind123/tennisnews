feedparser
# The launcher, from the SAME fork and commit the workflow downloads the binary
# from: coffeegrind123/camoufox-beta is JWriter20's main plus Web Serial, the
# setter seal, the daijro/camoufox#779 stock-parity sweep and the glxtest GPU
# probe. A launcher and a build that disagree about properties.json are not a
# combination anyone tests.
# [geoip] is not optional in practice: camoufox warns on every proxied launch
# that it is "heavily recommended", and without it the spoofed locale, timezone
# and geolocation describe a client somewhere other than the exit IP.
# Pinned to a commit: upstream history rewrites have broken an unpinned ref
# before (JWriter20 moved python/ to pythonlib/ on 2026-09-14).
camoufox[geoip] @ git+https://github.com/coffeegrind123/camoufox-beta.git@a96b87fd9ee9551ce79510a75194ea9b3a139982#subdirectory=pythonlib
# playwright 1.60 dispatches pageError with `location.url`, which the camoufox
# FF146 juggler never sends. The driver process then dies on the FIRST uncaught
# JS error on any page, taking the whole browser (and every remaining site) with
# it. 1.59 does not read that field.
playwright<1.60
